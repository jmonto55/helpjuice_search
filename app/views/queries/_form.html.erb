<%= form_with(model: query) do |form| %>
  <% if query.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(query.errors.count, "error") %> prohibited this query from being saved:</h2>

      <ul>
        <% query.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_ip, style: "display: block" %>
    <%= form.text_area :user_ip %>
  </div>

  <div>
    <%= form.label :search, style: "display: block" %>
    <%= form.text_area :search %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
